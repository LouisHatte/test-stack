# Test Stack App

## Run

```sh
pnpm install # install dependencies
npm run dev # run in dev mode
```

## Build

```sh
pnpm run build # build for production
pnpm run preview # run in production mode
```
